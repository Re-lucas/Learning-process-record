/**
 * 1．指出下面各种数据使用的合适数据类型（有些可使用多种数据类型）：

　　a．East Simpleton的人口
    a．int 类型，也可以是short 类型或unsigned short 类型。人口数是一个整数。

　　b．DVD影碟的价格
    b．float 类型，价格通常不是一个整数（也可以使用double 类型，但实际上不需要那么高的精度）。

　　c．本章出现次数最多的字母
    c.char 类型，字母是一个字符，可以使用char 类型来存储。

　　d．本章出现次数最多的字母次数
    d．int 类型，也可以是unsigned 类型。
    
    2．在什么情况下要用long类型的变量代替int类型的变量？
    * 当需要存储的整数值超过int类型的范围时，应使用long类型。通常，long类型用于处理更大的整数值，例如大于2,147,483,647（32位系统下int的最大值）的数值。

    3．使用哪些可移植的数据类型可以获得32位有符号整数？选择的理由是什么？
    * 可以使用`int`类型或`long`类型。
    选择`int`是因为它通常在大多数平台上保证为32位，而`long`在某些平台上可能是64位，因此使用`int`更具可移植性。
    C标准保证`int`至少为16位，但在大多数现代系统中，`int`通常为32位。

    4．指出下列常量的类型和含义（如果有的话）：
    a．char 类型常量（但是储存为int 类型）
    在 C 语言中，字符常量（如 'A'）虽然表示字符，但实际以 int 类型存储（占用 4 字节）。
    这是因为字符常量在编译时会被转换为对应的 ASCII 码值（整数）。

　  b．int 类型常量
    标准的整数常量（如 42、-7），直接表示整数值。
    默认范围通常为 -2147483648 到 2147483647（32 位系统）。
    
　  c．double 类型常量
    双精度浮点数常量（如 3.14、-0.5e-3）。
    默认带小数点的数字（如 2.0）或科学计数法（如 1.23e4）被视为 double 类型。

　  d．unsigned int 类型常量，十六进制格式
    以十六进制表示的无符号整数常量（如 0xFFFF、0x1A3Bu）。
    后缀 u 或 U 显式标记为无符号（例如 0x1234U）。

　  e．double 类型常量
    与 c 相同，但需注意：若常量包含后缀（如 f 或 F），则类型为 float（单精度）。

 */